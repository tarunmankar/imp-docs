MASTER PROMPT — BGM Track Repair Instructions

I will paste an existing BGM track entry. Repair ONLY the title and the prompt — do not output tags, SEO keywords, mood, genre, instrumentation, or anything else. Do not generate a new track from scratch, only fix the one I give you. No rule below may ever be broken, even partially.

1. TITLE
- Format: Title — Subtitle (em dash, not hyphen)
- Must not duplicate or closely match ANY title already used anywhere in this catalog, including every title already repaired earlier in this chat/session
- SEO-optimized: include the core instrument/genre keyword naturally (e.g. "Retro Guitar", "80s Synth", "Vintage Pop")

2. PROMPT (single flowing sentence, no headers/brackets)
- Structure: mood/genre tag → unique scene/visual description → instrument + texture description → BPM, key, time signature → instant opening hook (no slow fade-in, lands within first 2–5 seconds, never abrupt/jarring) → build → climax → resolve → use-case optimization line → "Purely instrumental. No vocals, no singer, no choir." → Duration: X:XX (always at the very end, always under 2:30)
- The finished prompt — scene description, wording, and sentence structure — must NOT match or closely resemble any prompt already repaired earlier in this same chat/session. Check against all previous entries before finalizing, not just the original source text.
- No bracket labels: [Core Emotion], [Instrumentation], [Technical Specs], etc.
- No timestamps: "Intro (0:00-0:30)", "Climax (1:15-2:00)"
- No mixing/mastering jargon: "studio-grade", "crystal-clear highs", "flawless mastering", "massive spatial mix"
- No real artist/song names or "in the style of X"
- No verbatim-reused boilerplate phrasing across tracks (e.g. don't always say "100% original bespoke motif" or "layered over warm mid-range textures, driving bassline, crisp rhythm") — reword every time
- If the scene description matches one already used anywhere in the catalog or chat history, replace it with a genuinely new, unique scene while keeping the same instrument/BPM/mood intent
- Rotate the musical key if recent tracks are repeating the same key too often
- Rotate chord progression / melodic shape if recent tracks feel structurally similar
- BPM must match the stated mood (e.g. "energetic/upbeat" should never carry 60–90 BPM)

Return ONLY this, nothing else:

**[Number]. [Title — Subtitle]**
**Prompt:** [repaired prompt]

After the entry, add one short note flagging anything the original broke (duplicate scene, duplicate title, banned language, key repetition, BPM mismatch) — never skip this check.
